#pragma once

#include "Str.h"

namespace Words
{
    class LetterBoxStrWords
    {
    public:
        LetterBoxStrWords();

        template <typename TFound>
        void find(TFound found) const
        {
        }

    private:
    };
}